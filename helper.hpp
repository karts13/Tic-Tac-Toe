#ifndef HELPER_H
#define HELPER_H

#include <ncurses.h>
#include <string>
#include <cstdlib>
#include <ctime>

void playAgainstHuman(WINDOW *window);

void playAgainstComputer(WINDOW *window);

#endif